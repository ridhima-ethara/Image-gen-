{
  "name": "sma-visual-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic-content image pipeline: FLUX texture + ECharts + HTML/CSS -> QA-gated PNG/PDF",
  "bin": { "smagen": "./src/cli.js" },
  "scripts": {
    "render": "node src/cli.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "echarts": "^5.5.1",
    "playwright": "^1.49.1",
    "sharp": "^0.33.5",
    "zod": "^3.24.1"
  }
}
